# Contributing

Use Node.js 22.12 or newer. Run `npm ci` and `npm run check` before submitting a pull request. Add a focused regression test for changes to the authentication flow. Use synthetic credentials in examples. Do not commit `.env`, credentials, tokens, or personal data. Report vulnerabilities privately as described in SECURITY.md.
